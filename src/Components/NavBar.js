const NavBar = () => {
    return (
        <nav>
            <nav>
                <a href="#">Inicio</a>
                <a href="#">Nosotros</a>
                <a href="#">Contacto</a>
            </nav>
        </nav>
    )
}

export default NavBar